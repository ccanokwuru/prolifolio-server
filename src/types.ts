enum ROLE {
  ARTIST = "artist",
  ADMIN = "admin",
  USER = "user",
}

enum MESSAGE {
  CHAT = "chat",
  REPLY = "reply",
  BROADCAST = "broadcast",
}

enum CONTACT {
  DEFAULT = "default",
  RESIDENTIAL = "residential",
  OFFICE = "office",
}

export { ROLE, MESSAGE, CONTACT };
